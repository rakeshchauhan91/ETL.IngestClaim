# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
.venv/
venv/

# Local secrets / env
.env

# Autogenerated sample data (regenerate via `docker compose run --rm seed-landing`)
data/landing/*.csv

# Docker/Postgres local volumes if ever bind-mounted
pgdata/

# OS/editor cruft
.DS_Store
.vscode/
.idea/

# Power BI - don't commit .pbix if it contains cached data/credentials
# (uncomment if your .pbix embeds imported data rather than just the report)
# *.pbix
